---
"next-safe-action": patch
"@next-safe-action/adapter-tanstack-query": minor
---

Add TanStack Query mutation adapter (`@next-safe-action/adapter-tanstack-query`) with `mutationOptions()` factory function. Export `isNavigationError`, `ThrowsErrorsBrand`, and `MaybeBrandThrows` from core package.
